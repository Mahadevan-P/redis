logging:
  level:
    com:
      nse:
        redis: DEBUG
server:
  port: 9002
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/redisdemo
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: mahadevan29
    username: root
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect 